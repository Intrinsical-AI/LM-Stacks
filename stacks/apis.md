# APIs

- **OpenRouter** – Unified access to nearly every major LLM provider via a single API.

- **Huggingface** – Unified access to OpenSoruce Models via a API.

- **Ollama** – Unified access to local LLMs via localhost API.
